# @reptiles-ge/img-compression

Native image compression for [Reptiles.ge](https://reptiles.ge).

A Sharp-based image optimization pipeline that turns untouched original photographs into
AVIF and WebP derivatives, stores them through a pluggable storage adapter, and delivers
them to the browser with correct responsive, LCP and layout-stability behaviour.

Documentation is in [`docs/`](docs/).
